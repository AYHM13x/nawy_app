import 'package:flutter/material.dart';

import 'add_images_body.dart';

class AddImagesView extends StatelessWidget {
  const AddImagesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: AddImagesBody(),
    );
  }
}
